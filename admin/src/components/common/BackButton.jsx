import React from "react";

const BackButton = () => {
    return (
        <button type="button" aria-label="Back" className="back-btn">
            <i className="icon-back"></i>
        </button>
    )
}

export default BackButton;